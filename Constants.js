export default class Constants {
    static spriteSize = 16;    // Default sprite size set to 16x16
    static velocityY = 0;      // Initial vertical velocity
    static gravity = 0.5;      // Gravity applied to entities

    static aspectRatio = 4;    // Aspect ratio for the game
    static borderSize = 20;    // Border size for the game canvas
}
